diff --git a/src/dashboard/public/css/style.css b/src/dashboard/public/css/style.css
index 73bb0ff..de48079 100644
--- a/src/dashboard/public/css/style.css
+++ b/src/dashboard/public/css/style.css
@@ -212,7 +212,7 @@ body {
 /* ÔöÇÔöÇÔöÇ Creator Grid ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 .creator-grid {
   display: grid;
-  grid-template-columns: 1fr 420px;
+  grid-template-columns: minmax(0, 1fr) minmax(360px, 420px);
   gap: 24px;
   align-items: start;
 }
@@ -220,8 +220,14 @@ body {
   .creator-grid { grid-template-columns: 1fr }
 }
 
+.creator-side-stack {
+  display: grid;
+  gap: 16px;
+}
+
 /* ÔöÇÔöÇÔöÇ Cards ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 .creator-form-card,
+.creator-tools-card,
 .creator-preview-card {
   background: var(--bg-secondary);
   border: 1px solid var(--border);
@@ -229,14 +235,31 @@ body {
   padding: 24px;
 }
 
+@media (min-width: 1001px) {
+  .creator-side-stack {
+    position: sticky;
+    top: 80px;
+    max-height: calc(100vh - 100px);
+    overflow-y: auto;
+    padding-right: 3px;
+  }
+
+  .creator-preview-card {
+    max-height: none;
+    overflow: visible;
+  }
+}
+
 /* ÔöÇÔöÇÔöÇ Type Tabs ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-.type-tabs {
+.type-tabs,
+.mode-tabs {
   display: flex;
   flex-wrap: wrap;
   gap: 6px;
   margin-top: 8px;
 }
-.type-tab {
+.type-tab,
+.mode-tab {
   padding: 7px 14px;
   background: var(--bg-card);
   border: 1px solid var(--border);
@@ -248,12 +271,14 @@ body {
   transition: var(--transition);
   white-space: nowrap;
 }
-.type-tab:hover {
+.type-tab:hover,
+.mode-tab:hover {
   border-color: var(--border-hover);
   color: var(--text-primary);
   background: var(--bg-input);
 }
-.type-tab.active {
+.type-tab.active,
+.mode-tab.active {
   background: rgba(220,20,60,.15);
   border-color: rgba(220,20,60,.5);
   color: var(--accent);
@@ -264,6 +289,49 @@ body {
 .form-group { margin-bottom: 18px }
 .form-group:last-of-type { margin-bottom: 0 }
 
+.editor-suite {
+  border-top: none;
+  border-bottom: none;
+  padding-top: 0;
+  padding-bottom: 0;
+}
+
+.form-disclosure {
+  border: 1px solid var(--border);
+  border-radius: var(--radius-sm);
+  background: rgba(0, 0, 0, 0.12);
+  overflow: hidden;
+}
+
+.disclosure-summary {
+  list-style: none;
+  cursor: pointer;
+  font-size: 13px;
+  font-weight: 700;
+  color: var(--text-secondary);
+  letter-spacing: 0.4px;
+  text-transform: uppercase;
+  padding: 11px 12px;
+}
+
+.disclosure-summary::-webkit-details-marker {
+  display: none;
+}
+
+.form-disclosure[open] .disclosure-summary {
+  border-bottom: 1px solid var(--border);
+}
+
+.disclosure-content {
+  padding: 12px;
+}
+
+.form-help {
+  color: var(--text-secondary);
+  font-size: 12px;
+  margin-bottom: 10px;
+}
+
 .form-label {
   display: block;
   font-size: 13px;
@@ -308,6 +376,290 @@ body {
 
 .form-textarea { resize: vertical; min-height: 100px; line-height: 1.6 }
 
+.form-checkbox {
+  display: flex;
+  align-items: center;
+  gap: 10px;
+  color: var(--text-secondary);
+  font-size: 13px;
+  margin-bottom: 10px;
+}
+
+.form-checkbox input {
+  accent-color: var(--accent);
+}
+
+.btn-secondary {
+  background: var(--bg-card);
+  border: 1px solid var(--border);
+  color: var(--text-secondary);
+  border-radius: var(--radius-sm);
+  padding: 10px 12px;
+  font-size: 12px;
+  font-weight: 600;
+  cursor: pointer;
+  transition: var(--transition);
+  white-space: nowrap;
+}
+
+.btn-secondary:hover {
+  border-color: var(--accent);
+  color: var(--accent);
+}
+
+.editor-tabs {
+  display: flex;
+  gap: 8px;
+  margin-bottom: 12px;
+}
+
+.editor-tab {
+  padding: 8px 12px;
+  border-radius: var(--radius-sm);
+  border: 1px solid var(--border);
+  background: var(--bg-card);
+  color: var(--text-secondary);
+  cursor: pointer;
+  font-size: 12px;
+  font-weight: 700;
+  transition: var(--transition);
+}
+
+.editor-tab:hover {
+  border-color: var(--border-hover);
+  color: var(--text-primary);
+}
+
+.editor-tab.active {
+  border-color: rgba(220,20,60,.5);
+  color: var(--accent);
+  background: rgba(220,20,60,.12);
+}
+
+.editor-panel {
+  display: flex;
+  flex-direction: column;
+  gap: 12px;
+}
+
+.format-toolbar {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 8px;
+}
+
+.toolbar-btn {
+  min-width: 38px;
+  height: 34px;
+  border-radius: var(--radius-sm);
+  border: 1px solid var(--border);
+  background: var(--bg-card);
+  color: var(--text-primary);
+  cursor: pointer;
+  font-size: 12px;
+  font-weight: 700;
+  transition: var(--transition);
+}
+
+.toolbar-btn:hover {
+  border-color: var(--accent);
+  color: var(--accent);
+  background: rgba(220,20,60,.12);
+}
+
+.toolbar-btn-utility {
+  min-width: 44px;
+}
+
+.mention-toolbox {
+  display: grid;
+  gap: 12px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-sm);
+  padding: 12px;
+  background: rgba(0, 0, 0, 0.14);
+}
+
+.editor-popovers {
+  margin-top: 10px;
+  display: grid;
+  gap: 10px;
+}
+
+.editor-popover {
+  border: 1px solid var(--border);
+  border-radius: var(--radius-sm);
+  background: rgba(0, 0, 0, 0.2);
+  padding: 10px;
+  display: grid;
+  gap: 10px;
+}
+
+.popover-header {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 8px;
+  color: var(--text-secondary);
+  font-size: 12px;
+  font-weight: 700;
+  text-transform: uppercase;
+  letter-spacing: 0.3px;
+}
+
+.popover-search {
+  width: 100%;
+  max-width: 230px;
+  background: var(--bg-input);
+  border: 1px solid var(--border);
+  border-radius: var(--radius-sm);
+  color: var(--text-primary);
+  font-size: 12px;
+  padding: 7px 9px;
+}
+
+.popover-search:focus {
+  outline: none;
+  border-color: var(--accent);
+  box-shadow: 0 0 0 3px var(--accent-glow);
+}
+
+.emoji-list,
+.mention-list,
+.mention-user-results {
+  display: flex;
+  flex-wrap: wrap;
+  gap: 8px;
+  max-height: 170px;
+  overflow-y: auto;
+}
+
+.mention-list-inline {
+  max-height: none;
+}
+
+.emoji-chip,
+.mention-chip,
+.mention-user-item {
+  border: 1px solid var(--border);
+  background: var(--bg-card);
+  color: var(--text-primary);
+  border-radius: var(--radius-sm);
+  cursor: pointer;
+  transition: var(--transition);
+}
+
+.emoji-chip:hover,
+.mention-chip:hover,
+.mention-user-item:hover {
+  border-color: var(--accent);
+  background: rgba(220,20,60,.12);
+}
+
+.emoji-chip {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  padding: 6px 8px;
+  font-size: 12px;
+}
+
+.emoji-chip img {
+  width: 20px;
+  height: 20px;
+  object-fit: contain;
+}
+
+.mention-section {
+  display: grid;
+  gap: 7px;
+}
+
+.mention-label {
+  color: var(--text-muted);
+  font-size: 11px;
+  font-weight: 700;
+  text-transform: uppercase;
+}
+
+.mention-chip {
+  padding: 6px 9px;
+  font-size: 12px;
+}
+
+.mention-list .mention-chip,
+.mention-user-results .mention-user-item {
+  width: 100%;
+  justify-content: flex-start;
+}
+
+.mention-list-inline .mention-chip {
+  width: auto;
+}
+
+.mention-broadcast {
+  color: #c9cdfb;
+}
+
+.mention-user-item {
+  display: flex;
+  flex-direction: column;
+  align-items: flex-start;
+  gap: 2px;
+  padding: 6px 8px;
+}
+
+.mention-user-name {
+  font-size: 12px;
+  color: var(--text-primary);
+}
+
+.mention-user-meta {
+  font-size: 11px;
+  color: var(--text-secondary);
+}
+
+.popover-empty {
+  color: var(--text-muted);
+  font-size: 12px;
+}
+
+.image-option-panel {
+  margin-top: 10px;
+}
+
+.upload-file-name {
+  margin-top: 8px;
+  color: var(--text-secondary);
+  font-size: 12px;
+}
+
+.hints-grid {
+  display: grid;
+  grid-template-columns: 1fr;
+  gap: 8px;
+}
+
+.hint-card {
+  display: flex;
+  align-items: center;
+  justify-content: space-between;
+  gap: 12px;
+  border: 1px solid var(--border);
+  border-radius: var(--radius-sm);
+  padding: 8px 10px;
+  color: var(--text-secondary);
+  font-size: 12px;
+}
+
+.hint-code {
+  font-family: 'Consolas', 'Courier New', monospace;
+  color: var(--text-primary);
+  background: rgba(255,255,255,.05);
+  border-radius: 4px;
+  padding: 2px 6px;
+}
+
 .select-wrapper { position: relative }
 .select-wrapper::after {
   content: 'Ôľż';
@@ -512,6 +864,41 @@ input[name="colorName"] { display: none }
 }
 .embed-field-value { font-size: 13px; color: #dbdee1; word-break: break-word }
 
+.discord-plain-message {
+  color: #dbdee1;
+  font-size: 14px;
+  line-height: 1.6;
+  word-break: break-word;
+  background: transparent;
+  padding-top: 2px;
+}
+
+.preview-image-placeholder {
+  margin-top: 8px;
+  color: var(--text-secondary);
+  font-size: 12px;
+}
+
+.preview-image-block {
+  margin-top: 10px;
+  display: grid;
+  gap: 6px;
+}
+
+.preview-image {
+  width: min(100%, 360px);
+  max-height: 280px;
+  object-fit: contain;
+  border-radius: var(--radius-sm);
+  border: 1px solid var(--border);
+  background: rgba(0, 0, 0, 0.18);
+}
+
+.preview-image-caption {
+  color: var(--text-secondary);
+  font-size: 12px;
+}
+
 /* ÔöÇÔöÇÔöÇ Toast ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 #toast-container {
   position: fixed;
@@ -605,6 +992,34 @@ input[name="colorName"] { display: none }
 .embed-description em     { font-style: italic }
 .embed-description u      { text-decoration: underline }
 .embed-description s      { text-decoration: line-through }
+.embed-description .md-h1-alt,
+.embed-description .md-h2,
+.embed-description .md-h3 {
+  display: block;
+  color: #fff;
+  font-weight: 700;
+  margin: 4px 0;
+}
+
+.embed-description .md-h1-alt { font-size: 17px }
+.embed-description .md-h2 { font-size: 15px }
+.embed-description .md-h3 { font-size: 14px }
+
+.md-mention {
+  display: inline-block;
+  background: rgba(88,101,242,.18);
+  color: #c9cdfb;
+  border-radius: 4px;
+  padding: 0 5px;
+  line-height: 1.5;
+}
+
+.md-discord-emoji {
+  width: 22px;
+  height: 22px;
+  object-fit: contain;
+  vertical-align: middle;
+}
 .md-code {
   background: rgba(0,0,0,.3);
   border-radius: 3px;
@@ -629,3 +1044,9 @@ input[name="colorName"] { display: none }
   margin: 4px 0;
   color: var(--text-secondary);
 }
+
+@media (max-width: 700px) {
+  .btn-secondary {
+    width: 100%;
+  }
+}
diff --git a/src/dashboard/public/js/app.js b/src/dashboard/public/js/app.js
index f868533..e62cdf6 100644
--- a/src/dashboard/public/js/app.js
+++ b/src/dashboard/public/js/app.js
@@ -1,74 +1,61 @@
-/* ÔöÇÔöÇÔöÇ Embed type definitions ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-const EMBED_TYPES = {
-  announcement: {
-    label: '­čôó Og┼éoszenie',
-    color: '#dc143c',
-    buildFields: () => [
-      { id: 'title',       label: 'Tytu┼é',   type: 'text',     required: true,  placeholder: 'np. Wa┼╝na informacja' },
-      { id: 'description', label: 'Tre┼Ť─ç',   type: 'textarea', required: true,  placeholder: '**Pogrubiony** tekst\n*Kursywa*\n`kod`' },
-      { id: 'colorName',   label: 'Kolor',   type: 'color-swatches' },
-    ],
-  },
-  welcome: {
-    label: '­čĹő Powitanie',
-    color: '#dc143c',
-    buildFields: () => [
-      { id: 'message',  label: 'Wiadomo┼Ť─ç powitalna', type: 'textarea', required: true, placeholder: 'Witamy Ci─Ö w szeregach Husarii!' },
-      { id: 'imageUrl', label: 'URL bannera',         type: 'text',     required: false, placeholder: 'https://...' },
-    ],
-  },
-  rulebook: {
-    label: '­čôő Regulamin',
-    color: '#dc143c',
-    buildFields: () => [
-      { id: 'rulesText', label: 'Tre┼Ť─ç regulaminu', type: 'textarea', required: true, rows: 8, placeholder: '1. Szanuj innych...' },
-    ],
-  },
-  zgloszenia: {
-    label: '­čôČ Zg┼éoszenia',
-    color: '#dc143c',
-    buildFields: () => [
-      { id: 'infoText', label: 'Tekst informacyjny', type: 'textarea', required: true, placeholder: 'Opis systemu zg┼éosze┼ä...' },
-    ],
-  },
-};
-
-const COLOR_MAP = { czerwony: '#dc143c', bia┼éy: '#f5f5f5', szary: '#99aab5', z┼éoty: '#ffd700' };
-
-/* ÔöÇÔöÇÔöÇ State ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-let currentType     = 'announcement';
-let selectedColor   = 'czerwony';
-let currentSection  = 'embed-creator';
-let selectedImage   = null;
-
-/* ÔöÇÔöÇÔöÇ Init ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+const COLOR_MAP = {
+  czerwony: '#dc143c',
+  bia┼éy: '#f5f5f5',
+  szary: '#99aab5',
+  z┼éoty: '#ffd700',
+}
+
+const MAX_UPLOAD_BYTES = 8 * 1024 * 1024
+const ALLOWED_UPLOAD_TYPES = new Set(['image/png', 'image/jpeg', 'image/gif'])
+const ALLOWED_UPLOAD_EXTS = new Set(['.png', '.jpg', '.jpeg', '.gif'])
+const UPLOAD_MIME_BY_EXT = {
+  '.png': 'image/png',
+  '.jpg': 'image/jpeg',
+  '.jpeg': 'image/jpeg',
+  '.gif': 'image/gif',
+}
+
+let currentMode = 'embedded'
+let selectedColor = 'czerwony'
+let activeEditorId = 'content-textarea'
+
+let channels = []
+let roles = []
+let images = []
+let mentionChannelResults = []
+let mentionRoleResults = []
+let mentionUserResults = []
+const knownUsers = new Map()
+
+let selectedImageName = null
+let selectedUploadFile = null
+let selectedUploadPreviewUrl = null
+let mentionChannelSearchDebounceId = null
+let mentionRoleSearchDebounceId = null
+let mentionUserSearchDebounceId = null
+let mentionChannelSearchRequestId = 0
+let mentionRoleSearchRequestId = 0
+let mentionUserSearchRequestId = 0
+let embedSectionBound = false
+
 document.addEventListener('DOMContentLoaded', () => {
-  initSidebarNav();
-  switchSection(currentSection);
-  loadUserInfo();
-});
-
-/* ÔöÇÔöÇÔöÇ Sidebar navigation ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function initSidebarNav() {
-  document.querySelectorAll('.sidebar-item[data-section]').forEach(item => {
-    item.addEventListener('click', (e) => {
-      e.preventDefault();
-      const section = item.dataset.section;
-      switchSection(section);
-    });
-  });
-}
-
-/* ÔöÇÔöÇÔöÇ User info ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
+  loadUserInfo()
+  initEmbedSection()
+})
+
 async function loadUserInfo() {
   try {
-    const resp = await fetch('/api/me');
-    if (!resp.ok) { window.location.href = '/auth/discord'; return; }
-    const { user } = await resp.json();
-    const container = document.getElementById('navbar-user');
+    const resp = await fetch('/api/me')
+    if (!resp.ok) {
+      window.location.href = '/auth/discord'
+      return
+    }
+
+    const { user } = await resp.json()
+    const container = document.getElementById('navbar-user')
     const avatarUrl = user.avatar
       ? `https://cdn.discordapp.com/avatars/${user.id}/${user.avatar}.png?size=64`
-      : null;
+      : null
 
     container.innerHTML = `
       <div class="user-info">
@@ -77,397 +64,973 @@ async function loadUserInfo() {
           : `<div class="user-avatar-placeholder">­čĹĄ</div>`}
         <span class="user-name">${escapeHtml(user.globalName || user.username)}</span>
         <a href="/auth/logout" class="btn-logout">Wyloguj</a>
-      </div>`;
+      </div>`
   } catch {
-    window.location.href = '/auth/discord';
+    window.location.href = '/auth/discord'
+  }
+}
+
+async function initEmbedSection() {
+  await Promise.all([
+    loadChannels(),
+    loadRoles(),
+    loadImages(),
+  ])
+
+  renderChannelSelector()
+  renderPingRoleSelector()
+  renderImageLibrary(images)
+  renderMentionChannelResults([])
+  renderMentionRoleResults([])
+  renderMentionUserResults([])
+
+  updateModeUI()
+  updateImagePanels()
+  updatePreview()
+  updateSendButton()
+
+  if (!embedSectionBound) {
+    embedSectionBound = true
+    bindEmbedSectionListeners()
+  }
+}
+
+function bindEmbedSectionListeners() {
+  const modeTabs = document.getElementById('mode-tabs')
+  modeTabs?.addEventListener('click', (event) => {
+    const button = event.target.closest('.mode-tab')
+    if (!button) return
+
+    const nextMode = button.dataset.mode
+    if (!nextMode || nextMode === currentMode) return
+
+    currentMode = nextMode
+    updateModeUI()
+    updatePreview()
+    updateSendButton()
+  })
+
+  const toolbar = document.getElementById('format-toolbar')
+  toolbar?.addEventListener('click', (event) => {
+    const button = event.target.closest('.toolbar-btn')
+    if (!button) return
+
+    const wrap = button.dataset.wrap
+    const prefix = button.dataset.prefix
+
+    if (wrap) {
+      wrapSelection(wrap)
+      return
+    }
+
+    if (prefix) {
+      prefixSelectionLines(prefix)
+    }
+  })
+
+  const mentionQuickList = document.getElementById('mention-quick-list')
+  const mentionChannelResults = document.getElementById('mention-channel-results')
+  const mentionRoleResults = document.getElementById('mention-role-results')
+  const mentionChannelSearch = document.getElementById('mention-channel-search')
+  const mentionRoleSearch = document.getElementById('mention-role-search')
+  const mentionUserSearch = document.getElementById('mention-user-search')
+  const mentionUserResults = document.getElementById('mention-user-results')
+
+  mentionQuickList?.addEventListener('click', (event) => {
+    const button = event.target.closest('[data-token]')
+    const token = button?.dataset.token
+    if (!token) return
+
+    insertToken(token)
+  })
+
+  mentionChannelResults?.addEventListener('click', (event) => {
+    const button = event.target.closest('[data-token]')
+    const token = button?.dataset.token
+    if (!token) return
+
+    insertToken(token)
+  })
+
+  mentionRoleResults?.addEventListener('click', (event) => {
+    const button = event.target.closest('[data-token]')
+    const token = button?.dataset.token
+    if (!token) return
+
+    insertToken(token)
+  })
+
+  mentionUserResults?.addEventListener('click', (event) => {
+    const button = event.target.closest('[data-token]')
+    const token = button?.dataset.token
+    if (!token) return
+
+    insertToken(token)
+  })
+
+  mentionChannelSearch?.addEventListener('input', () => {
+    if (mentionChannelSearchDebounceId) {
+      clearTimeout(mentionChannelSearchDebounceId)
+    }
+
+    mentionChannelSearchDebounceId = setTimeout(async () => {
+      await searchMentionChannels(mentionChannelSearch.value)
+    }, 220)
+  })
+
+  mentionRoleSearch?.addEventListener('input', () => {
+    if (mentionRoleSearchDebounceId) {
+      clearTimeout(mentionRoleSearchDebounceId)
+    }
+
+    mentionRoleSearchDebounceId = setTimeout(async () => {
+      await searchMentionRoles(mentionRoleSearch.value)
+    }, 220)
+  })
+
+  mentionUserSearch?.addEventListener('input', () => {
+    if (mentionUserSearchDebounceId) {
+      clearTimeout(mentionUserSearchDebounceId)
+    }
+
+    mentionUserSearchDebounceId = setTimeout(async () => {
+      await searchMentionUsers(mentionUserSearch.value)
+    }, 220)
+  })
+
+  const pingToggle = document.getElementById('ping-role-enabled')
+  const pingSelect = document.getElementById('ping-role-select')
+  pingToggle?.addEventListener('change', () => {
+    pingSelect.disabled = !pingToggle.checked
+    updateSendButton()
+  })
+  pingSelect?.addEventListener('change', updateSendButton)
+
+  const imageModeSelect = document.getElementById('image-mode-select')
+  imageModeSelect?.addEventListener('change', () => {
+    updateImagePanels()
+    updatePreview()
+    updateSendButton()
+  })
+
+  const uploadInput = document.getElementById('image-upload-input')
+  uploadInput?.addEventListener('change', () => {
+    const files = uploadInput.files
+    selectedUploadFile = files && files[0] ? files[0] : null
+
+    if (selectedUploadFile && !isAllowedUploadFile(selectedUploadFile)) {
+      showToast('Dozwolone formaty pliku: JPEG, PNG, GIF.', 'error')
+      uploadInput.value = ''
+      selectedUploadFile = null
+      clearUploadPreviewUrl()
+    }
+
+    if (selectedUploadFile && selectedUploadFile.size > MAX_UPLOAD_BYTES) {
+      showToast('Plik jest za du┼╝y. Maksymalny rozmiar to 8 MB.', 'error')
+      uploadInput.value = ''
+      selectedUploadFile = null
+      clearUploadPreviewUrl()
+    }
+
+    if (selectedUploadFile) {
+      clearUploadPreviewUrl()
+      selectedUploadPreviewUrl = URL.createObjectURL(selectedUploadFile)
+    } else {
+      clearUploadPreviewUrl()
+    }
+
+    const fileNameElement = document.getElementById('upload-file-name')
+    fileNameElement.textContent = selectedUploadFile
+      ? `Wybrano: ${selectedUploadFile.name}`
+      : 'Nie wybrano pliku.'
+
+    updatePreview()
+    updateSendButton()
+  })
+
+  const titleInput = document.getElementById('title')
+  const contentTextarea = document.getElementById('content-textarea')
+  const channelSelect = document.getElementById('channel-select')
+
+  const updateHandler = () => {
+    updatePreview()
+    updateSendButton()
   }
+
+  titleInput?.addEventListener('input', updateHandler)
+  titleInput?.addEventListener('focus', () => {
+    activeEditorId = 'title'
+  })
+
+  contentTextarea?.addEventListener('input', updateHandler)
+  contentTextarea?.addEventListener('focus', () => {
+    activeEditorId = 'content-textarea'
+  })
+
+  channelSelect?.addEventListener('change', updateSendButton)
+
+  document.querySelectorAll('.color-swatch').forEach((swatch) => {
+    swatch.addEventListener('click', () => {
+      const nextColor = swatch.dataset.color
+      if (!nextColor) return
+
+      selectedColor = nextColor
+      document.querySelectorAll('.color-swatch').forEach((element) => {
+        element.classList.remove('active')
+      })
+      swatch.classList.add('active')
+      updatePreview()
+    })
+  })
+
+  const sendButton = document.getElementById('send-btn')
+  sendButton?.addEventListener('click', publishMessage)
+
+  window.addEventListener('beforeunload', () => {
+    clearUploadPreviewUrl()
+  })
 }
 
-/* ÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉ
-   EMBED CREATOR
-   ÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉ */
-function initEmbedSection() {
-  loadChannels('channel-select');
-  renderTypeTabs();
-  renderFields(currentType);
-  initTypeTabListeners();
-  initSendButton();
+async function loadChannels() {
+  try {
+    const resp = await fetch('/api/channels')
+    if (!resp.ok) throw new Error('fetch failed')
+
+    const json = await resp.json()
+    channels = Array.isArray(json.channels) ? json.channels : []
+  } catch {
+    channels = []
+    showToast('Nie uda┼éo si─Ö pobra─ç kana┼é├│w.', 'error')
+  }
 }
 
-/* ÔöÇÔöÇÔöÇ Channels ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-async function loadChannels(selectId) {
-  const select = document.getElementById(selectId);
-  if (!select) return;
+async function loadRoles() {
   try {
-    const resp = await fetch('/api/channels');
-    if (!resp.ok) throw new Error('fetch failed');
-    const { channels } = await resp.json();
+    const resp = await fetch('/api/roles')
+    if (!resp.ok) throw new Error('fetch failed')
 
-    select.innerHTML = `<option value="">ÔÇö wybierz kana┼é ÔÇö</option>` +
-      channels.map(c => `<option value="${c.id}">#${escapeHtml(c.name)}</option>`).join('');
+    const json = await resp.json()
+    roles = Array.isArray(json.roles) ? json.roles : []
+  } catch {
+    roles = []
+    showToast('Nie uda┼éo si─Ö pobra─ç r├│l.', 'error')
+  }
+}
+
+async function loadImages() {
+  try {
+    const resp = await fetch('/api/images')
+    if (!resp.ok) throw new Error('fetch failed')
 
-    select.disabled = false;
-    if (selectId === 'channel-select') {
-      updateSendButton();
-      select.addEventListener('change', updateSendButton);
+    const json = await resp.json()
+    images = Array.isArray(json.images) ? json.images : []
+  } catch {
+    images = []
+    showToast('Nie uda┼éo si─Ö pobra─ç obraz├│w z /img.', 'error')
+  }
+}
+
+function renderMentionChannelResults(results) {
+  const container = document.getElementById('mention-channel-results')
+  if (!container) return
+
+  if (!results.length) {
+    container.innerHTML = '<p class="popover-empty">Wpisz min. 2 znaki, aby wyszuka─ç kana┼é.</p>'
+    return
+  }
+
+  container.innerHTML = results.map((channel) => (
+    `<button type="button" class="mention-chip" data-token="&lt;#${channel.id}&gt;">#${escapeHtml(channel.name)}</button>`
+  )).join('')
+}
+
+function renderMentionRoleResults(results) {
+  const container = document.getElementById('mention-role-results')
+  if (!container) return
+
+  if (!results.length) {
+    container.innerHTML = '<p class="popover-empty">Wpisz min. 2 znaki, aby wyszuka─ç rol─Ö.</p>'
+    return
+  }
+
+  container.innerHTML = results.map((role) => (
+    `<button type="button" class="mention-chip" data-token="&lt;@&${role.id}&gt;">@${escapeHtml(role.name)}</button>`
+  )).join('')
+}
+
+async function searchMentionChannels(rawQuery) {
+  const query = String(rawQuery ?? '').trim()
+  mentionChannelSearchRequestId += 1
+  const requestId = mentionChannelSearchRequestId
+
+  if (query.length < 2) {
+    mentionChannelResults = []
+    renderMentionChannelResults([])
+    return
+  }
+
+  try {
+    const resp = await fetch(`/api/channels/search?query=${encodeURIComponent(query)}`)
+    if (!resp.ok) throw new Error('fetch failed')
+
+    const json = await resp.json()
+    if (requestId !== mentionChannelSearchRequestId) {
+      return
     }
-    if (selectId === 'img-channel-select') {
-      updateImageSendButton();
-      select.addEventListener('change', updateImageSendButton);
+
+    mentionChannelResults = Array.isArray(json.channels) ? json.channels : []
+    renderMentionChannelResults(mentionChannelResults)
+  } catch {
+    if (requestId !== mentionChannelSearchRequestId) {
+      return
     }
+
+    mentionChannelResults = []
+    renderMentionChannelResults([])
+    showToast('Nie uda┼éo si─Ö wyszuka─ç kana┼é├│w.', 'error')
+  }
+}
+
+async function searchMentionRoles(rawQuery) {
+  const query = String(rawQuery ?? '').trim()
+  mentionRoleSearchRequestId += 1
+  const requestId = mentionRoleSearchRequestId
+
+  if (query.length < 2) {
+    mentionRoleResults = []
+    renderMentionRoleResults([])
+    return
+  }
+
+  try {
+    const resp = await fetch(`/api/roles/search?query=${encodeURIComponent(query)}`)
+    if (!resp.ok) throw new Error('fetch failed')
+
+    const json = await resp.json()
+    if (requestId !== mentionRoleSearchRequestId) {
+      return
+    }
+
+    mentionRoleResults = Array.isArray(json.roles) ? json.roles : []
+    renderMentionRoleResults(mentionRoleResults)
   } catch {
-    select.innerHTML = `<option value="">B┼é─ůd pobierania kana┼é├│w</option>`;
-    showToast('Nie uda┼éo si─Ö pobra─ç kana┼é├│w.', 'error');
+    if (requestId !== mentionRoleSearchRequestId) {
+      return
+    }
+
+    mentionRoleResults = []
+    renderMentionRoleResults([])
+    showToast('Nie uda┼éo si─Ö wyszuka─ç r├│l.', 'error')
   }
 }
 
-/* ÔöÇÔöÇÔöÇ Type tabs ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function renderTypeTabs() {
-  const tabs = document.getElementById('type-tabs');
-  if (!tabs) return;
-  tabs.innerHTML = Object.entries(EMBED_TYPES).map(([key, def]) =>
-    `<button class="type-tab${key === currentType ? ' active' : ''}" data-type="${key}">${def.label}</button>`
-  ).join('');
+function renderMentionUserResults(results) {
+  const container = document.getElementById('mention-user-results')
+  if (!container) return
+
+  if (!results.length) {
+    container.innerHTML = '<p class="popover-empty">Wpisz min. 2 znaki, aby wyszuka─ç u┼╝ytkownika.</p>'
+    return
+  }
+
+  container.innerHTML = results.map((member) => {
+    const displayName = member.nick || member.globalName || member.username || 'u┼╝ytkownik'
+    return `
+      <button type="button" class="mention-user-item" data-token="&lt;@${member.id}&gt;" title="@${escapeHtml(displayName)}">
+        <span class="mention-user-name">${escapeHtml(displayName)}</span>
+        <span class="mention-user-meta">@${escapeHtml(member.username || 'unknown')}</span>
+      </button>`
+  }).join('')
 }
 
-function initTypeTabListeners() {
-  const tabs = document.getElementById('type-tabs');
-  if (!tabs) return;
-  tabs.addEventListener('click', (e) => {
-    const btn = e.target.closest('.type-tab');
-    if (!btn) return;
-    const type = btn.dataset.type;
-    if (type === currentType) return;
+async function searchMentionUsers(rawQuery) {
+  const query = String(rawQuery ?? '').trim()
+  mentionUserSearchRequestId += 1
+  const requestId = mentionUserSearchRequestId
+
+  if (query.length < 2) {
+    mentionUserResults = []
+    renderMentionUserResults([])
+    return
+  }
+
+  try {
+    const resp = await fetch(`/api/members/search?query=${encodeURIComponent(query)}`)
+    if (!resp.ok) throw new Error('fetch failed')
+
+    const json = await resp.json()
+    if (requestId !== mentionUserSearchRequestId) {
+      return
+    }
+
+    mentionUserResults = Array.isArray(json.members) ? json.members : []
 
-    currentType  = type;
-    selectedColor = 'czerwony';
+    mentionUserResults.forEach((member) => {
+      if (!member?.id) return
+      const displayName = member.nick || member.globalName || member.username || 'u┼╝ytkownik'
+      knownUsers.set(member.id, displayName)
+    })
 
-    document.querySelectorAll('.type-tab').forEach(t => t.classList.remove('active'));
-    btn.classList.add('active');
+    renderMentionUserResults(mentionUserResults)
+  } catch {
+    if (requestId !== mentionUserSearchRequestId) {
+      return
+    }
 
-    renderFields(type);
-    updatePreview();
-  });
+    mentionUserResults = []
+    renderMentionUserResults([])
+    showToast('Nie uda┼éo si─Ö wyszuka─ç u┼╝ytkownik├│w.', 'error')
+  }
 }
 
-/* ÔöÇÔöÇÔöÇ Dynamic fields ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function renderFields(type) {
-  const container = document.getElementById('dynamic-fields');
-  if (!container) return;
-  const defs = EMBED_TYPES[type]?.buildFields() ?? [];
+function renderChannelSelector() {
+  const select = document.getElementById('channel-select')
+  if (!select) return
+
+  const previous = select.value
+  select.innerHTML = [
+    '<option value="">ÔÇö wybierz kana┼é ÔÇö</option>',
+    ...channels.map((channel) => `<option value="${channel.id}">#${escapeHtml(channel.name)}</option>`),
+  ].join('')
 
-  container.innerHTML = defs.map(field => buildFieldHtml(field)).join('');
+  select.value = channels.some((channel) => channel.id === previous) ? previous : ''
+  select.disabled = channels.length === 0
+}
 
-  container.querySelectorAll('input, textarea, select').forEach(el => {
-    el.addEventListener('input', updatePreview);
-    el.addEventListener('change', updatePreview);
-  });
+function renderPingRoleSelector() {
+  const select = document.getElementById('ping-role-select')
+  if (!select) return
 
-  container.querySelectorAll('.color-swatch').forEach(sw => {
-    sw.addEventListener('click', () => {
-      selectedColor = sw.dataset.color;
-      container.querySelectorAll('.color-swatch').forEach(s => s.classList.remove('active'));
-      sw.classList.add('active');
-      updatePreview();
-    });
-    if (sw.dataset.color === selectedColor) sw.classList.add('active');
-  });
+  const previous = select.value
+  select.innerHTML = [
+    '<option value="">Wybierz rol─Ö do pingowania</option>',
+    ...roles.map((role) => `<option value="${role.id}">@${escapeHtml(role.name)}</option>`),
+  ].join('')
 
-  updatePreview();
+  select.value = roles.some((role) => role.id === previous) ? previous : ''
 }
 
-function buildFieldHtml(field) {
-  const optional = !field.required ? `<span class="optional">(opcjonalnie)</span>` : '';
+function renderImageLibrary(imageList) {
+  const grid = document.getElementById('image-grid')
+  if (!grid) return
+
+  if (!imageList.length) {
+    grid.innerHTML = '<p class="img-empty">Brak obraz├│w w folderze /img.</p>'
+    return
+  }
+
+  grid.innerHTML = imageList.map((name) => `
+    <div class="img-card${selectedImageName === name ? ' selected' : ''}" data-name="${escapeHtml(name)}" title="${escapeHtml(name)}">
+      <img src="/img/${encodeURIComponent(name)}" alt="${escapeHtml(name)}" loading="lazy">
+      <span class="img-card-name">${escapeHtml(name)}</span>
+    </div>`).join('')
+
+  grid.querySelectorAll('.img-card').forEach((card) => {
+    card.addEventListener('click', () => {
+      const nextName = card.dataset.name
+      if (!nextName) return
+
+      selectedImageName = nextName
+      selectedUploadFile = null
+
+      const uploadInput = document.getElementById('image-upload-input')
+      if (uploadInput) {
+        uploadInput.value = ''
+      }
+
+      const fileNameElement = document.getElementById('upload-file-name')
+      if (fileNameElement) {
+        fileNameElement.textContent = 'Nie wybrano pliku.'
+      }
+
+      renderImageLibrary(images)
+      updatePreview()
+      updateSendButton()
+    })
+  })
+}
+
+function updateModeUI() {
+  document.querySelectorAll('.mode-tab').forEach((tab) => {
+    tab.classList.toggle('active', tab.dataset.mode === currentMode)
+  })
+
+  const titleGroup = document.getElementById('title-group')
+  const colorGroup = document.getElementById('color-group')
 
-  if (field.type === 'color-swatches') {
-    return `<div class="form-group">
-      <label class="form-label">Kolor embeda</label>
-      <div class="color-swatches">
-        <div class="color-swatch" data-color="czerwony" title="Czerwony"></div>
-        <div class="color-swatch" data-color="bia┼éy"    title="Bia┼éy"></div>
-        <div class="color-swatch" data-color="szary"    title="Szary"></div>
-        <div class="color-swatch" data-color="z┼éoty"    title="Z┼éoty"></div>
-      </div>
-    </div>`;
+  const isEmbedded = currentMode === 'embedded'
+  if (titleGroup) {
+    titleGroup.style.display = isEmbedded ? '' : 'none'
   }
+  if (colorGroup) {
+    colorGroup.style.display = isEmbedded ? '' : 'none'
+  }
+
+  const titleInput = document.getElementById('title')
+  if (!isEmbedded && titleInput) {
+    titleInput.value = ''
+  }
+}
 
-  const rows        = field.rows      ? ` rows="${field.rows}"` : ' rows="4"';
-  const placeholder = field.placeholder ? ` placeholder="${escapeHtml(field.placeholder)}"` : '';
+function updateImagePanels() {
+  const mode = document.getElementById('image-mode-select')?.value ?? 'none'
+  const libraryPanel = document.getElementById('image-library-panel')
+  const uploadPanel = document.getElementById('image-upload-panel')
+
+  if (libraryPanel) {
+    libraryPanel.hidden = mode !== 'library'
+  }
+  if (uploadPanel) {
+    uploadPanel.hidden = mode !== 'upload'
+  }
 
-  if (field.type === 'textarea') {
-    return `<div class="form-group">
-      <label class="form-label" for="${field.id}">${field.label} ${optional}</label>
-      <textarea id="${field.id}" name="${field.id}" class="form-textarea"${rows}${placeholder}></textarea>
-    </div>`;
+  if (mode !== 'library') {
+    selectedImageName = null
+    renderImageLibrary(images)
   }
 
-  return `<div class="form-group">
-    <label class="form-label" for="${field.id}">${field.label} ${optional}</label>
-    <input type="${field.type}" id="${field.id}" name="${field.id}" class="form-input"${placeholder}>
-  </div>`;
+  if (mode !== 'upload') {
+    selectedUploadFile = null
+    clearUploadPreviewUrl()
+    const uploadInput = document.getElementById('image-upload-input')
+    if (uploadInput) {
+      uploadInput.value = ''
+    }
+
+    const fileNameElement = document.getElementById('upload-file-name')
+    if (fileNameElement) {
+      fileNameElement.textContent = 'Nie wybrano pliku.'
+    }
+  }
 }
 
-/* ÔöÇÔöÇÔöÇ Preview ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 function updatePreview() {
-  const data = collectFormData();
-  const def  = EMBED_TYPES[currentType];
-
-  let barColor = def.color;
-  if (currentType === 'announcement') barColor = COLOR_MAP[selectedColor] ?? def.color;
-  document.getElementById('embed-color-bar').style.background = barColor;
-
-  const desc   = document.getElementById('preview-description');
-  const fields = document.getElementById('preview-fields');
-
-  switch (currentType) {
-    case 'announcement':
-      desc.innerHTML   = renderPreviewText(data.title, data.description);
-      fields.innerHTML = '';
-      break;
-    case 'welcome':
-      desc.innerHTML   = renderPreviewText('Witaj na Husarii!', data.message);
-      fields.innerHTML = '';
-      break;
-    case 'rulebook':
-      desc.innerHTML   = renderPreviewText('Regulamin serwera G2 Hussars', data.rulesText);
-      fields.innerHTML = '';
-      break;
-    case 'zgloszenia':
-      desc.innerHTML   = renderPreviewText('Zg┼éoszenia', data.infoText);
-      fields.innerHTML = '';
-      break;
-  }
-
-  updateSendButton();
-}
-
-function renderPreviewText(title, body) {
-  const titleHtml = title
-    ? `<span class="embed-h1">${escapeHtml(title)}</span>`
-    : '';
-  const bodyHtml = body
-    ? renderMarkdown(body)
-    : '<span style="opacity:.45">Wype┼énij formularzÔÇŽ</span>';
-  return titleHtml + bodyHtml;
-}
-
-function previewFields(defs) {
-  return defs.map(f => `
-    <div class="embed-field${f.inline ? '' : ' full'}">
-      <div class="embed-field-name">${escapeHtml(f.name)}</div>
-      <div class="embed-field-value">${escapeHtml(String(f.value))}</div>
-    </div>`).join('');
-}
-
-/* ÔöÇÔöÇÔöÇ Markdown renderer (Discord subset) ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function renderMarkdown(text) {
-  if (!text) return '';
-
-  // Escape HTML first, then apply markdown on top
-  let html = escapeHtml(text);
-
-  // Code blocks (```ÔÇŽ```)
-  html = html.replace(/```([\s\S]*?)```/g, '<pre class="md-codeblock">$1</pre>');
-  // Inline code (`ÔÇŽ`)
-  html = html.replace(/`([^`\n]+)`/g, '<code class="md-code">$1</code>');
-  // Bold+italic (***ÔÇŽ***)
-  html = html.replace(/\*\*\*(.+?)\*\*\*/g, '<strong><em>$1</em></strong>');
-  // Bold (**ÔÇŽ**)
-  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>');
-  // Italic (*ÔÇŽ* or _ÔÇŽ_)
-  html = html.replace(/\*([^*\n]+)\*/g, '<em>$1</em>');
-  html = html.replace(/_([^_\n]+)_/g, '<em>$1</em>');
-  // Underline (__ÔÇŽ__)
-  html = html.replace(/__(.+?)__/g, '<u>$1</u>');
-  // Strikethrough (~~ÔÇŽ~~)
-  html = html.replace(/~~(.+?)~~/g, '<s>$1</s>');
-  // Block quote (> ÔÇŽ)
-  html = html.replace(/^&gt; (.+)$/gm, '<blockquote class="md-quote">$1</blockquote>');
-  // Newlines Ôćĺ <br>
-  html = html.replace(/\n/g, '<br>');
-
-  return html;
-}
-
-/* ÔöÇÔöÇÔöÇ Collect form data ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function collectFormData() {
-  const data = { type: currentType, colorName: selectedColor };
-  document.querySelectorAll('#dynamic-fields input, #dynamic-fields textarea').forEach(el => {
-    data[el.id] = el.value;
-  });
-  data.channelId = document.getElementById('channel-select')?.value ?? '';
-  return data;
-}
-
-/* ÔöÇÔöÇÔöÇ Send button ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function updateSendButton() {
-  const btn     = document.getElementById('send-btn');
-  const channel = document.getElementById('channel-select')?.value;
-  if (btn) btn.disabled = !channel;
+  const data = collectFormDataSync()
+  const embedContainer = document.getElementById('embed-preview-container')
+  const messageContainer = document.getElementById('message-preview-container')
+  const previewDescription = document.getElementById('preview-description')
+  const colorBar = document.getElementById('embed-color-bar')
+  const imageBlock = document.getElementById('preview-image-block')
+  const imageElement = document.getElementById('preview-image')
+  const imageCaption = document.getElementById('preview-image-caption')
+  const imagePlaceholder = document.getElementById('preview-image-placeholder')
+
+  if (
+    !embedContainer
+    || !messageContainer
+    || !previewDescription
+    || !colorBar
+    || !imageBlock
+    || !imageElement
+    || !imageCaption
+    || !imagePlaceholder
+  ) {
+    return
+  }
+
+  if (data.mode === 'embedded') {
+    embedContainer.style.display = 'flex'
+    messageContainer.style.display = 'none'
+    colorBar.style.background = COLOR_MAP[selectedColor] ?? COLOR_MAP.czerwony
+    previewDescription.innerHTML = renderPreviewEmbedText(data.title, data.content)
+  } else {
+    embedContainer.style.display = 'none'
+    messageContainer.style.display = ''
+    messageContainer.innerHTML = renderMarkdown(data.content) || '<span style="opacity:.45">Wpisz tre┼Ť─ç publikacji.</span>'
+  }
+
+  let previewImageSrc = ''
+  let previewImageCaption = ''
+
+  if (data.imageMode === 'library' && data.imageFilename) {
+    previewImageSrc = `/img/${encodeURIComponent(data.imageFilename)}`
+    previewImageCaption = `Grafika z biblioteki: ${data.imageFilename}`
+  }
+
+  if (data.imageMode === 'upload' && selectedUploadPreviewUrl) {
+    previewImageSrc = selectedUploadPreviewUrl
+    previewImageCaption = selectedUploadFile ? `Wgrana grafika: ${selectedUploadFile.name}` : 'Wgrana grafika'
+  }
+
+  if (previewImageSrc) {
+    imageElement.src = previewImageSrc
+    imageCaption.textContent = previewImageCaption
+    imageBlock.style.display = ''
+    imagePlaceholder.style.display = 'none'
+  } else {
+    imageBlock.style.display = 'none'
+    imageElement.src = ''
+    imageCaption.textContent = ''
+
+    if (data.imageMode === 'none') {
+      imagePlaceholder.style.display = 'none'
+    } else if (data.imageMode === 'library') {
+      imagePlaceholder.textContent = '­čľ╝´ŞĆ Wybierz grafik─Ö z biblioteki, aby zobaczy─ç podgl─ůd.'
+      imagePlaceholder.style.display = ''
+    } else {
+      imagePlaceholder.textContent = '­čľ╝´ŞĆ Wgraj plik, aby zobaczy─ç podgl─ůd.'
+      imagePlaceholder.style.display = ''
+    }
+  }
+}
+
+function renderPreviewEmbedText(title, content) {
+  const titleHtml = title?.trim()
+    ? `<span class="embed-h1">${renderInlineText(title.trim())}</span>`
+    : ''
+
+  const bodyHtml = content?.trim()
+    ? renderMarkdown(content)
+    : '<span style="opacity:.45">Wpisz tre┼Ť─ç publikacji.</span>'
+
+  return `${titleHtml}${bodyHtml}`
+}
+
+function renderInlineText(value) {
+  let html = escapeHtml(value)
+  html = renderDiscordCustomEmojis(html)
+  html = renderDiscordMentions(html)
+  return html
 }
 
-function initSendButton() {
-  document.getElementById('send-btn')?.addEventListener('click', sendEmbed);
+function collectFormDataSync() {
+  const pingEnabled = document.getElementById('ping-role-enabled')?.checked ?? false
+  const pingRoleId = document.getElementById('ping-role-select')?.value ?? ''
+  const imageMode = document.getElementById('image-mode-select')?.value ?? 'none'
+
+  return {
+    mode: currentMode,
+    channelId: document.getElementById('channel-select')?.value ?? '',
+    title: document.getElementById('title')?.value ?? '',
+    content: document.getElementById('content-textarea')?.value ?? '',
+    colorName: selectedColor,
+    mentionRoleEnabled: pingEnabled,
+    mentionRoleId: pingEnabled ? pingRoleId : '',
+    imageMode,
+    imageFilename: imageMode === 'library' ? (selectedImageName ?? '') : '',
+  }
+}
+
+async function collectFormData() {
+  const syncData = collectFormDataSync()
+
+  if (syncData.imageMode !== 'upload' || !selectedUploadFile) {
+    return {
+      ...syncData,
+      uploadFileName: '',
+      uploadMimeType: '',
+      uploadBase64: '',
+    }
+  }
+
+  const dataUrl = await fileToDataUrl(selectedUploadFile)
+
+  const uploadMimeType = normalizeUploadMimeType(selectedUploadFile.type, selectedUploadFile.name)
+
+  return {
+    ...syncData,
+    uploadFileName: selectedUploadFile.name,
+    uploadMimeType,
+    uploadBase64: dataUrl,
+  }
 }
 
-async function sendEmbed() {
-  const btn     = document.getElementById('send-btn');
-  const btnText = document.getElementById('send-btn-text');
-  const data    = collectFormData();
+function updateSendButton() {
+  const button = document.getElementById('send-btn')
+  if (!button) return
 
-  btn.disabled = true;
-  btn.classList.add('loading');
-  btnText.textContent = 'Wysy┼éanieÔÇŽ';
+  const data = collectFormDataSync()
+
+  const hasChannel = !!data.channelId
+  const hasContent = !!data.content.trim()
+  const pingReady = !data.mentionRoleEnabled || !!data.mentionRoleId
+
+  const imageReady = data.imageMode === 'none'
+    || (data.imageMode === 'library' && !!data.imageFilename)
+    || (data.imageMode === 'upload' && !!selectedUploadFile)
+
+  button.disabled = !(hasChannel && hasContent && pingReady && imageReady)
+}
+
+async function publishMessage() {
+  const button = document.getElementById('send-btn')
+  const buttonText = document.getElementById('send-btn-text')
+  if (!button || !buttonText) return
+
+  button.disabled = true
+  button.classList.add('loading')
+  buttonText.textContent = 'Publikowanie...'
 
   try {
+    const payload = await collectFormData()
+
     const resp = await fetch('/api/embed', {
       method: 'POST',
       headers: { 'Content-Type': 'application/json' },
-      body: JSON.stringify(data),
-    });
+      body: JSON.stringify(payload),
+    })
 
-    const json = await resp.json();
-    if (!resp.ok) throw new Error(json.error ?? 'Nieznany b┼é─ůd');
+    const json = await parseApiResponse(resp)
+    if (!resp.ok) {
+      throw new Error(json.error ?? 'Nieznany b┼é─ůd')
+    }
+
+    if (Array.isArray(json.warnings) && json.warnings.length > 0) {
+      json.warnings.forEach((warning) => {
+        showToast(`ÔÜá´ŞĆ ${warning}`, 'info')
+      })
+    }
 
-    showToast('Ôťů Embed wys┼éany pomy┼Ťlnie!', 'success');
-  } catch (err) {
-    showToast(`ÔŁî ${err.message}`, 'error');
+    if (payload.imageMode === 'upload') {
+      await loadImages()
+      renderImageLibrary(images)
+    }
+
+    showToast('Ôťů Publikacja wys┼éana pomy┼Ťlnie!', 'success')
+  } catch (error) {
+    const message = error instanceof Error ? error.message : 'Nieznany b┼é─ůd'
+    showToast(`ÔŁî ${message}`, 'error')
   } finally {
-    btn.classList.remove('loading');
-    btnText.textContent = 'Wy┼Ťlij embed';
-    updateSendButton();
+    button.classList.remove('loading')
+    buttonText.textContent = 'Opublikuj'
+    updateSendButton()
   }
 }
 
-/* ÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉ
-   IMAGE SENDER
-   ÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉÔĽÉ */
-async function initImageSection() {
-  const sendButton = document.getElementById('img-send-btn');
-  if (sendButton) {
-    sendButton.addEventListener('click', sendImage);
+async function parseApiResponse(resp) {
+  const contentType = resp.headers.get('content-type') ?? ''
+  if (contentType.includes('application/json')) {
+    return resp.json().catch(() => ({}))
   }
 
-  await Promise.all([
-    loadChannels('img-channel-select'),
-    loadImages(),
-  ]);
+  const text = await resp.text()
+  if (text) {
+    return { error: text }
+  }
+
+  return {}
 }
 
-async function loadImages() {
-  const grid = document.getElementById('image-grid');
-  if (!grid) return;
-  grid.innerHTML = '<p class="img-loading">┼üadowanie obraz├│wÔÇŽ</p>';
+async function fileToDataUrl(file) {
+  return new Promise((resolve, reject) => {
+    const reader = new FileReader()
+    reader.onload = () => resolve(String(reader.result ?? ''))
+    reader.onerror = () => reject(new Error('Nie uda┼éo si─Ö odczyta─ç pliku.'))
+    reader.readAsDataURL(file)
+  })
+}
 
-  try {
-    const resp = await fetch('/api/images');
-    if (!resp.ok) throw new Error('fetch failed');
-    const { images } = await resp.json();
+function insertToken(token) {
+  const target = getActiveEditor()
+  if (!target) {
+    showToast('Najpierw kliknij pole tekstowe edytora.', 'info')
+    return
+  }
 
-    if (!images.length) {
-      grid.innerHTML = '<p class="img-empty">Brak obraz├│w w folderze /img.</p>';
-      return;
-    }
+  const value = target.value ?? ''
+  const start = target.selectionStart ?? value.length
+  const end = target.selectionEnd ?? value.length
 
-    grid.innerHTML = images.map(name => `
-      <div class="img-card" data-name="${escapeHtml(name)}" title="${escapeHtml(name)}">
-        <img src="/img/${encodeURIComponent(name)}" alt="${escapeHtml(name)}" loading="lazy">
-        <span class="img-card-name">${escapeHtml(name)}</span>
-      </div>`).join('');
-
-    grid.querySelectorAll('.img-card').forEach(card => {
-      card.addEventListener('click', () => {
-        grid.querySelectorAll('.img-card').forEach(c => c.classList.remove('selected'));
-        card.classList.add('selected');
-        selectedImage = card.dataset.name;
-        updateImageSendButton();
-      });
-    });
-  } catch {
-    grid.innerHTML = '<p class="img-error">B┼é─ůd pobierania obraz├│w.</p>';
-    showToast('Nie uda┼éo si─Ö pobra─ç listy obraz├│w.', 'error');
+  const rightPart = value.slice(end)
+  const needsTrailingSpace = rightPart.length === 0 || !/^\s/.test(rightPart)
+  const insertion = `${token}${needsTrailingSpace ? ' ' : ''}`
+
+  target.value = `${value.slice(0, start)}${insertion}${value.slice(end)}`
+
+  const caretPos = start + insertion.length
+  target.setSelectionRange(caretPos, caretPos)
+  target.dispatchEvent(new Event('input', { bubbles: true }))
+  target.focus()
+}
+
+function clearUploadPreviewUrl() {
+  if (selectedUploadPreviewUrl) {
+    URL.revokeObjectURL(selectedUploadPreviewUrl)
+    selectedUploadPreviewUrl = null
   }
 }
 
-function updateImageSendButton() {
-  const btn     = document.getElementById('img-send-btn');
-  const channel = document.getElementById('img-channel-select')?.value;
-  if (btn) btn.disabled = !(selectedImage && channel);
+function getActiveEditor() {
+  const active = document.getElementById(activeEditorId)
+  if (active && (active.tagName === 'TEXTAREA' || active.tagName === 'INPUT')) {
+    return active
+  }
+
+  const fallback = document.getElementById('content-textarea')
+  if (fallback) {
+    activeEditorId = 'content-textarea'
+  }
+
+  return fallback
 }
 
-async function sendImage() {
-  const btn     = document.getElementById('img-send-btn');
-  const btnText = document.getElementById('img-send-btn-text');
-  const channel = document.getElementById('img-channel-select')?.value;
+function wrapSelection(token) {
+  const target = getActiveEditor()
+  if (!target) {
+    showToast('Najpierw kliknij pole tekstowe edytora.', 'info')
+    return
+  }
 
-  if (!selectedImage || !channel) return;
+  const value = target.value ?? ''
+  const start = target.selectionStart ?? value.length
+  const end = target.selectionEnd ?? value.length
+  const selected = value.slice(start, end)
 
-  btn.disabled = true;
-  btn.classList.add('loading');
-  btnText.textContent = 'Wysy┼éanieÔÇŽ';
+  const wrapped = selected
+    ? `${token}${selected}${token}`
+    : `${token}${token}`
 
-  try {
-    const resp = await fetch('/api/send-image', {
-      method: 'POST',
-      headers: { 'Content-Type': 'application/json' },
-      body: JSON.stringify({ filename: selectedImage, channelId: channel }),
-    });
+  target.value = `${value.slice(0, start)}${wrapped}${value.slice(end)}`
 
-    const json = await resp.json();
-    if (!resp.ok) throw new Error(json.error ?? 'Nieznany b┼é─ůd');
+  const caretPos = selected ? start + wrapped.length : start + token.length
+  target.setSelectionRange(caretPos, caretPos)
+  target.dispatchEvent(new Event('input', { bubbles: true }))
+  target.focus()
+}
 
-    showToast('Ôťů Obraz wys┼éany pomy┼Ťlnie!', 'success');
-  } catch (err) {
-    showToast(`ÔŁî ${err.message}`, 'error');
-  } finally {
-    btn.classList.remove('loading');
-    btnText.textContent = 'Wy┼Ťlij obraz';
-    updateImageSendButton();
+function prefixSelectionLines(prefix) {
+  const target = getActiveEditor()
+  if (!target) {
+    showToast('Najpierw kliknij pole tekstowe edytora.', 'info')
+    return
   }
+
+  const value = target.value ?? ''
+  const selectionStart = target.selectionStart ?? value.length
+  const selectionEnd = target.selectionEnd ?? value.length
+  const start = value.lastIndexOf('\n', selectionStart - 1) + 1
+  const endBreak = value.indexOf('\n', selectionEnd)
+  const end = endBreak === -1 ? value.length : endBreak
+
+  const block = value.slice(start, end)
+  const prefixed = block
+    .split('\n')
+    .map((line) => `${prefix}${line}`)
+    .join('\n')
+
+  target.value = `${value.slice(0, start)}${prefixed}${value.slice(end)}`
+  target.setSelectionRange(start, start + prefixed.length)
+  target.dispatchEvent(new Event('input', { bubbles: true }))
+  target.focus()
 }
 
-/* ÔöÇÔöÇÔöÇ Section init hook ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-const SECTION_INIT = {
-  'embed-creator': initEmbedSection,
-  'image-sender':  initImageSection,
-};
-const _sectionInitialized = new Set();
+function renderMarkdown(text) {
+  if (!text) return ''
+
+  const escaped = escapeHtml(text)
+  const codeBlocks = []
+
+  let html = escaped.replace(/```([\s\S]*?)```/g, (_match, code) => {
+    const index = codeBlocks.push(`<pre class="md-codeblock">${code}</pre>`) - 1
+    return `@@CODEBLOCK_${index}@@`
+  })
 
-function switchSection(section) {
-  currentSection = section;
-  document.querySelectorAll('.sidebar-item').forEach(i => i.classList.remove('active'));
-  const active = document.querySelector(`.sidebar-item[data-section="${section}"]`);
-  if (active) active.classList.add('active');
+  html = html.replace(/^###\s+(.+)$/gm, '<span class="md-h3">$1</span>')
+  html = html.replace(/^##\s+(.+)$/gm, '<span class="md-h2">$1</span>')
+  html = html.replace(/^#\s+(.+)$/gm, '<span class="md-h1-alt">$1</span>')
+  html = html.replace(/^&gt;\s?(.+)$/gm, '<blockquote class="md-quote">$1</blockquote>')
 
-  document.querySelectorAll('.section-panel').forEach(p => p.style.display = 'none');
-  const panel = document.getElementById(`section-${section}`);
-  if (panel) panel.style.display = '';
+  html = html.replace(/`([^`\n]+)`/g, '<code class="md-code">$1</code>')
+  html = html.replace(/\*\*\*(.+?)\*\*\*/g, '<strong><em>$1</em></strong>')
+  html = html.replace(/\*\*(.+?)\*\*/g, '<strong>$1</strong>')
+  html = html.replace(/__(.+?)__/g, '<u>$1</u>')
+  html = html.replace(/~~(.+?)~~/g, '<s>$1</s>')
+  html = html.replace(/\*([^*\n]+)\*/g, '<em>$1</em>')
+  html = html.replace(/_([^_\n]+)_/g, '<em>$1</em>')
 
-  if (!_sectionInitialized.has(section) && SECTION_INIT[section]) {
-    _sectionInitialized.add(section);
-    SECTION_INIT[section]();
+  html = renderDiscordCustomEmojis(html)
+  html = renderDiscordMentions(html)
+
+  html = html.replace(/\n/g, '<br>')
+  html = html.replace(/@@CODEBLOCK_(\d+)@@/g, (_match, index) => codeBlocks[Number(index)] ?? '')
+
+  return html
+}
+
+function renderDiscordCustomEmojis(text) {
+  return text.replace(/&lt;(a?):([a-zA-Z0-9_]+):(\d{17,20})&gt;/g, (_match, animatedFlag, name, id) => {
+    const ext = animatedFlag === 'a' ? 'gif' : 'png'
+    const src = `https://cdn.discordapp.com/emojis/${id}.${ext}?size=32&quality=lossless`
+    return `<img class="md-discord-emoji" src="${src}" alt=":${name}:" title=":${name}:">`
+  })
+}
+
+function renderDiscordMentions(text) {
+  const channelMap = new Map(channels.map((channel) => [channel.id, channel.name]))
+  const roleMap = new Map(roles.map((role) => [role.id, role.name]))
+
+  let html = text.replace(/&lt;#(\d{17,20})&gt;/g, (_match, id) => {
+    const name = channelMap.get(id) ?? 'kana┼é'
+    return `<span class="md-mention">#${escapeHtml(name)}</span>`
+  })
+
+  html = html.replace(/&lt;@&(\d{17,20})&gt;/g, (_match, id) => {
+    const name = roleMap.get(id) ?? 'rola'
+    return `<span class="md-mention">@${escapeHtml(name)}</span>`
+  })
+
+  html = html.replace(/&lt;@!?(\d{17,20})&gt;/g, (_match, id) => {
+    const name = knownUsers.get(id) ?? 'u┼╝ytkownik'
+    return `<span class="md-mention">@${escapeHtml(name)}</span>`
+  })
+
+  html = html.replace(/(^|[\s(>])@everyone\b/g, '$1<span class="md-mention">@everyone</span>')
+  html = html.replace(/(^|[\s(>])@here\b/g, '$1<span class="md-mention">@here</span>')
+
+  return html
+}
+
+function fileExtension(filename) {
+  const lower = (filename ?? '').toLowerCase()
+  const dotIndex = lower.lastIndexOf('.')
+  return dotIndex === -1 ? '' : lower.slice(dotIndex)
+}
+
+function normalizeUploadMimeType(mimeType, filename) {
+  const normalizedMimeType = (mimeType ?? '').trim().toLowerCase()
+
+  if (normalizedMimeType === 'image/jpeg' || normalizedMimeType === 'image/jpg') {
+    return 'image/jpeg'
   }
+
+  if (normalizedMimeType === 'image/png' || normalizedMimeType === 'image/gif') {
+    return normalizedMimeType
+  }
+
+  const ext = fileExtension(filename)
+  return UPLOAD_MIME_BY_EXT[ext] ?? ''
+}
+
+function isAllowedUploadFile(file) {
+  const normalizedMimeType = normalizeUploadMimeType(file.type, file.name)
+  if (ALLOWED_UPLOAD_TYPES.has(normalizedMimeType)) {
+    return true
+  }
+
+  return ALLOWED_UPLOAD_EXTS.has(fileExtension(file.name))
 }
 
-/* ÔöÇÔöÇÔöÇ Toast ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
 function showToast(message, type = 'info') {
-  const container = document.getElementById('toast-container');
-  const toast = document.createElement('div');
-  toast.className = `toast toast-${type}`;
-  toast.textContent = message;
-  container.appendChild(toast);
+  const container = document.getElementById('toast-container')
+  if (!container) return
+
+  const toast = document.createElement('div')
+  toast.className = `toast toast-${type}`
+  toast.textContent = message
+  container.appendChild(toast)
+
   setTimeout(() => {
-    toast.style.animation = 'fadeOut 0.3s ease forwards';
-    setTimeout(() => toast.remove(), 300);
-  }, 3500);
+    toast.style.animation = 'fadeOut 0.3s ease forwards'
+    setTimeout(() => toast.remove(), 300)
+  }, 3500)
 }
 
-/* ÔöÇÔöÇÔöÇ Utils ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ */
-function escapeHtml(str) {
-  if (!str) return '';
-  return String(str)
+function escapeHtml(value) {
+  if (!value) return ''
+
+  return String(value)
     .replace(/&/g, '&amp;')
     .replace(/</g, '&lt;')
     .replace(/>/g, '&gt;')
-    .replace(/"/g, '&quot;');
+    .replace(/"/g, '&quot;')
 }
diff --git a/src/dashboard/routes/api.ts b/src/dashboard/routes/api.ts
index 3ae6387..b470bb8 100644
--- a/src/dashboard/routes/api.ts
+++ b/src/dashboard/routes/api.ts
@@ -1,13 +1,122 @@
 import { Router } from 'express';
 import { config } from 'dotenv';
+import { randomUUID } from 'crypto';
+import { mkdir, unlink, writeFile } from 'fs/promises';
+import { join } from 'path';
 import { requireAuth } from '../middleware/require-auth.js';
-import { getGuildTextChannels, sendEmbedToChannel, listImages, sendImageToChannel } from '../discord-api.js';
-import { buildEmbedJson, validateEmbedForm, type EmbedFormData } from '../embed-handlers.js';
+import {
+    deleteChannelMessage,
+    getGuildTextChannels,
+    getGuildRoles,
+    getGuildEmojis,
+    searchGuildMembers,
+    sendMessageToChannel,
+    listImages,
+    sendImageToChannel,
+} from '../discord-api.js';
+import {
+    buildDashboardAllowedMentions,
+    buildDashboardPingPayload,
+    buildDashboardMessagePayload,
+    buildEmbedJson,
+    validateEmbedForm,
+    type EmbedFormData,
+} from '../embed-handlers.js';
 
 config();
 
 export const apiRouter = Router();
 
+const ALLOWED_UPLOAD_MIME = new Set([
+    'image/png',
+    'image/jpeg',
+    'image/gif',
+]);
+const MAX_UPLOAD_BYTES = 8 * 1024 * 1024;
+const MIME_EXTENSION: Record<string, string> = {
+    'image/png': '.png',
+    'image/jpeg': '.jpg',
+    'image/gif': '.gif',
+};
+
+interface PreparedUpload {
+    buffer: Buffer;
+    mimeType: string;
+}
+
+function imgDirPath(): string {
+    return join(__dirname, '..', '..', '..', 'img');
+}
+
+function normalizeUploadMimeType(mimeType: string): string {
+    const normalized = mimeType.trim().toLowerCase();
+    if (normalized === 'image/jpg') {
+        return 'image/jpeg';
+    }
+    return normalized;
+}
+
+async function saveUploadToImageLibrary(buffer: Buffer, mimeType: string): Promise<string> {
+    const ext = MIME_EXTENSION[mimeType];
+    if (!ext) {
+        throw new Error('Unsupported upload MIME type.');
+    }
+
+    const filename = `${randomUUID()}${ext}`;
+    const targetDir = imgDirPath();
+
+    await mkdir(targetDir, { recursive: true });
+    await writeFile(join(targetDir, filename), buffer);
+
+    return filename;
+}
+
+function parseUploadData(uploadBase64: string, uploadMimeType: string): Buffer | null {
+    const trimmed = uploadBase64.trim();
+    const dataUrlMatch = /^data:([a-zA-Z0-9.+-]+\/[a-zA-Z0-9.+-]+);base64,([A-Za-z0-9+/=]+)$/.exec(trimmed);
+
+    let base64Data = trimmed;
+    if (dataUrlMatch) {
+        base64Data = dataUrlMatch[2] ?? '';
+    }
+
+    const normalized = base64Data.replace(/\s+/g, '');
+    if (!normalized || normalized.length % 4 !== 0 || !/^[A-Za-z0-9+/]+=*$/.test(normalized)) {
+        return null;
+    }
+
+    return Buffer.from(normalized, 'base64');
+}
+
+function detectImageMime(buffer: Buffer): string | null {
+    if (
+        buffer.length >= 8
+        && buffer[0] === 0x89
+        && buffer[1] === 0x50
+        && buffer[2] === 0x4e
+        && buffer[3] === 0x47
+        && buffer[4] === 0x0d
+        && buffer[5] === 0x0a
+        && buffer[6] === 0x1a
+        && buffer[7] === 0x0a
+    ) {
+        return 'image/png';
+    }
+
+    if (buffer.length >= 3 && buffer[0] === 0xff && buffer[1] === 0xd8 && buffer[2] === 0xff) {
+        return 'image/jpeg';
+    }
+
+    if (buffer.length >= 6) {
+        const gifHeader = buffer.toString('ascii', 0, 6);
+        if (gifHeader === 'GIF87a' || gifHeader === 'GIF89a') {
+            return 'image/gif';
+        }
+    }
+
+    return null;
+}
+
 apiRouter.use(requireAuth);
 
 // GET /api/me ÔÇö current user info
@@ -27,6 +136,106 @@ apiRouter.get('/channels', async (_req, res) => {
     }
 });
 
+// GET /api/channels/search ÔÇö search channels for mention picker
+apiRouter.get('/channels/search', async (req, res) => {
+    const guildId = process.env.GUILD_ID!;
+    const query = typeof req.query.query === 'string' ? req.query.query.trim().toLowerCase() : '';
+
+    if (query.length < 2) {
+        res.json({ channels: [] });
+        return;
+    }
+
+    const rawLimit = Number.parseInt(String(req.query.limit ?? '20'), 10);
+    const limit = Number.isFinite(rawLimit) ? Math.max(1, Math.min(50, rawLimit)) : 20;
+
+    try {
+        const channels = await getGuildTextChannels(guildId);
+        const filteredChannels = channels
+            .filter((channel) => channel.name.toLowerCase().includes(query))
+            .slice(0, limit);
+
+        res.json({ channels: filteredChannels });
+    } catch (err) {
+        console.error('Failed to search channels:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö wyszuka─ç kana┼é├│w.' });
+    }
+});
+
+// GET /api/roles ÔÇö live role list from Discord
+apiRouter.get('/roles', async (_req, res) => {
+    const guildId = process.env.GUILD_ID!;
+
+    try {
+        const roles = await getGuildRoles(guildId);
+        res.json({ roles });
+    } catch (err) {
+        console.error('Failed to fetch roles:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö pobra─ç listy r├│l.' });
+    }
+});
+
+// GET /api/roles/search ÔÇö search roles for mention picker
+apiRouter.get('/roles/search', async (req, res) => {
+    const guildId = process.env.GUILD_ID!;
+    const query = typeof req.query.query === 'string' ? req.query.query.trim().toLowerCase() : '';
+
+    if (query.length < 2) {
+        res.json({ roles: [] });
+        return;
+    }
+
+    const rawLimit = Number.parseInt(String(req.query.limit ?? '20'), 10);
+    const limit = Number.isFinite(rawLimit) ? Math.max(1, Math.min(50, rawLimit)) : 20;
+
+    try {
+        const roles = await getGuildRoles(guildId);
+        const filteredRoles = roles
+            .filter((role) => role.name.toLowerCase().includes(query))
+            .slice(0, limit);
+
+        res.json({ roles: filteredRoles });
+    } catch (err) {
+        console.error('Failed to search roles:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö wyszuka─ç r├│l.' });
+    }
+});
+
+// GET /api/emojis ÔÇö live emoji list from Discord
+apiRouter.get('/emojis', async (_req, res) => {
+    const guildId = process.env.GUILD_ID!;
+
+    try {
+        const emojis = await getGuildEmojis(guildId);
+        res.json({ emojis });
+    } catch (err) {
+        console.error('Failed to fetch emojis:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö pobra─ç listy emotek.' });
+    }
+});
+
+// GET /api/members/search ÔÇö search guild members for mention picker
+apiRouter.get('/members/search', async (req, res) => {
+    const guildId = process.env.GUILD_ID!;
+    const query = typeof req.query.query === 'string' ? req.query.query.trim() : '';
+
+    if (query.length < 2) {
+        res.json({ members: [] });
+        return;
+    }
+
+    const rawLimit = Number.parseInt(String(req.query.limit ?? '8'), 10);
+    const limit = Number.isFinite(rawLimit) ? Math.max(1, Math.min(20, rawLimit)) : 8;
+
+    try {
+        const members = await searchGuildMembers(guildId, query, limit);
+        res.json({ members });
+    } catch (err) {
+        console.error('Failed to search members:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö wyszuka─ç u┼╝ytkownik├│w.' });
+    }
+});
+
 // GET /api/images ÔÇö list available images from /img directory
 apiRouter.get('/images', (_req, res) => {
     try {
@@ -67,7 +276,12 @@ apiRouter.post('/send-image', async (req, res) => {
 
 // POST /api/embed ÔÇö build & send embed
 apiRouter.post('/embed', async (req, res) => {
-    const data = req.body as EmbedFormData;
+    const data = {
+        ...(req.body as EmbedFormData),
+        mode: req.body?.mode,
+        imageMode: req.body?.imageMode,
+        mentionRoleEnabled: req.body?.mentionRoleEnabled === true || req.body?.mentionRoleEnabled === 'true',
+    } as EmbedFormData;
 
     const validationError = validateEmbedForm(data);
     if (validationError) {
@@ -81,11 +295,164 @@ apiRouter.post('/embed', async (req, res) => {
     }
 
     try {
-        const embedJson = buildEmbedJson(data);
-        const messageId = await sendEmbedToChannel(data.channelId, embedJson);
-        res.json({ success: true, messageId });
+        let publishData: EmbedFormData = { ...data };
+        let warnings: string[] = [];
+
+        if (publishData.mentionRoleEnabled && publishData.mentionRoleId) {
+            try {
+                const roles = await getGuildRoles(process.env.GUILD_ID!);
+                const targetRole = roles.find((role) => role.id === publishData.mentionRoleId);
+
+                if (!targetRole) {
+                    warnings = [...warnings, 'Wybrana rola do pingu nie istnieje. Publikacja zosta┼éa wys┼éana bez pingu.'];
+                    publishData = {
+                        ...publishData,
+                        mentionRoleEnabled: false,
+                        mentionRoleId: '',
+                    };
+                }
+            } catch (roleErr) {
+                console.error('Failed to verify ping role:', roleErr);
+                warnings = [...warnings, 'Nie uda┼éo si─Ö zweryfikowa─ç roli do pingu. Publikacja zosta┼éa wys┼éana bez pingu.'];
+                publishData = {
+                    ...publishData,
+                    mentionRoleEnabled: false,
+                    mentionRoleId: '',
+                };
+            }
+        }
+
+        if (publishData.imageMode === 'library' && publishData.imageFilename) {
+            const availableImages = listImages();
+            if (!availableImages.includes(publishData.imageFilename)) {
+                res.status(400).json({ error: 'Wybrany obraz z biblioteki nie istnieje.' });
+                return;
+            }
+        }
+
+        let preparedUpload: PreparedUpload | null = null;
+        if (
+            publishData.imageMode === 'upload'
+            && publishData.uploadBase64
+            && publishData.uploadFileName
+            && publishData.uploadMimeType
+        ) {
+            const normalizedUploadMimeType = normalizeUploadMimeType(publishData.uploadMimeType);
+
+            if (!ALLOWED_UPLOAD_MIME.has(normalizedUploadMimeType)) {
+                res.status(400).json({ error: 'Nieobs┼éugiwany format pliku graficznego.' });
+                return;
+            }
+
+            const imageBuffer = parseUploadData(publishData.uploadBase64, normalizedUploadMimeType);
+            if (!imageBuffer || !imageBuffer.length) {
+                res.status(400).json({ error: 'Wgrany plik graficzny ma nieprawid┼éowy format.' });
+                return;
+            }
+
+            if (imageBuffer.length > MAX_UPLOAD_BYTES) {
+                res.status(400).json({ error: 'Wgrany plik jest za du┼╝y (max 8 MB).' });
+                return;
+            }
+
+            const detectedMime = detectImageMime(imageBuffer);
+            if (!detectedMime || detectedMime !== normalizedUploadMimeType) {
+                res.status(400).json({ error: 'Zawarto┼Ť─ç pliku nie zgadza si─Ö z typem obrazu.' });
+                return;
+            }
+
+            preparedUpload = {
+                buffer: imageBuffer,
+                mimeType: detectedMime,
+            };
+        }
+
+        const pingPayload = buildDashboardPingPayload(publishData);
+        let messageId: string | undefined;
+        let pingMessageId: string | undefined;
+        let imageMessageId: string | undefined;
+
+        if (pingPayload.content) {
+            try {
+                pingMessageId = await sendMessageToChannel(publishData.channelId, pingPayload);
+            } catch (pingErr) {
+                console.error('Failed to send role ping:', pingErr);
+                warnings = [...warnings, 'Nie uda┼éo si─Ö wys┼éa─ç pingu roli. G┼é├│wna publikacja zosta┼éa wys┼éana bez pingu.'];
+            }
+        }
+
+        if (publishData.mode === 'embedded') {
+            const publisherName = req.session.user?.globalName
+                ?? req.session.user?.username
+                ?? 'Administrator';
+            const publisherId = req.session.user?.id;
+            const embedJson = buildEmbedJson(publishData, {
+                publishedBy: publisherName,
+                publishedByUserId: publisherId,
+            });
+            const normalizedContent = publishData.content?.trim() ?? '';
+
+            try {
+                messageId = await sendMessageToChannel(publishData.channelId, {
+                    embeds: [embedJson],
+                    allowed_mentions: buildDashboardAllowedMentions(normalizedContent),
+                });
+            } catch (messageErr) {
+                if (pingMessageId) {
+                    await deleteChannelMessage(publishData.channelId, pingMessageId).catch((deleteErr) => {
+                        console.error('Failed to rollback ping message:', deleteErr);
+                    });
+                }
+                throw messageErr;
+            }
+        } else {
+            const messagePayload = buildDashboardMessagePayload(publishData);
+            try {
+                messageId = await sendMessageToChannel(publishData.channelId, messagePayload);
+            } catch (messageErr) {
+                if (pingMessageId) {
+                    await deleteChannelMessage(publishData.channelId, pingMessageId).catch((deleteErr) => {
+                        console.error('Failed to rollback ping message:', deleteErr);
+                    });
+                }
+                throw messageErr;
+            }
+        }
+
+        if (publishData.imageMode === 'library' && publishData.imageFilename) {
+            try {
+                imageMessageId = await sendImageToChannel(publishData.channelId, publishData.imageFilename);
+            } catch (imageErr) {
+                console.error('Failed to send library image:', imageErr);
+                warnings = [...warnings, 'Wiadomo┼Ť─ç zosta┼éa opublikowana, ale nie uda┼éo si─Ö wys┼éa─ç grafiki z biblioteki.'];
+            }
+        }
+
+        if (publishData.imageMode === 'upload' && preparedUpload) {
+            let storedFilename: string | null = null;
+            try {
+                storedFilename = await saveUploadToImageLibrary(preparedUpload.buffer, preparedUpload.mimeType);
+                imageMessageId = await sendImageToChannel(publishData.channelId, storedFilename);
+            } catch (imageErr) {
+                if (storedFilename) {
+                    await unlink(join(imgDirPath(), storedFilename)).catch((deleteErr) => {
+                        console.error('Failed to cleanup stored upload after send error:', deleteErr);
+                    });
+                }
+                console.error('Failed to send uploaded image:', imageErr);
+                warnings = [...warnings, 'Wiadomo┼Ť─ç zosta┼éa opublikowana, ale nie uda┼éo si─Ö wys┼éa─ç wgranej grafiki.'];
+            }
+        }
+
+        res.json({
+            success: true,
+            messageId,
+            pingMessageId,
+            imageMessageId,
+            warnings,
+        });
     } catch (err) {
-        console.error('Failed to send embed:', err);
-        res.status(500).json({ error: 'Nie uda┼éo si─Ö wys┼éa─ç embeda.' });
+        console.error('Failed to publish message:', err);
+        res.status(500).json({ error: 'Nie uda┼éo si─Ö opublikowa─ç wiadomo┼Ťci.' });
     }
 });
diff --git a/src/dashboard/views/dashboard.html b/src/dashboard/views/dashboard.html
index 6585717..2480ca3 100644
--- a/src/dashboard/views/dashboard.html
+++ b/src/dashboard/views/dashboard.html
@@ -8,7 +8,6 @@
 </head>
 <body>
 
-<!-- ÔöÇÔöÇ NAVBAR ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
 <nav class="navbar">
   <div class="navbar-brand">
     <img src="/img/hussars_logo_80x80.png" class="navbar-logo" alt="HusariaBot">
@@ -20,61 +19,89 @@
   </div>
 </nav>
 
-<!-- ÔöÇÔöÇ MAIN ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
 <main class="main">
-
-  <!-- Sidebar -->
   <aside class="sidebar">
     <nav class="sidebar-nav">
       <a href="#" class="sidebar-item active" data-section="embed-creator">
         <span class="sidebar-icon">­čÄĘ</span>
-        <span>Kreator Embeda</span>
-      </a>
-      <a href="#" class="sidebar-item" data-section="image-sender">
-        <span class="sidebar-icon">­čľ╝´ŞĆ</span>
-        <span>Wy┼Ťlij obraz</span>
-      </a>
-      <a href="#" class="sidebar-item disabled" title="Wkr├│tce">
-        <span class="sidebar-icon">­čÄč´ŞĆ</span>
-        <span>Tickety</span>
-        <span class="badge-soon">wkr├│tce</span>
-      </a>
-      <a href="#" class="sidebar-item disabled" title="Wkr├│tce">
-        <span class="sidebar-icon">­čôŐ</span>
-        <span>Statystyki</span>
-        <span class="badge-soon">wkr├│tce</span>
+        <span>Kreator publikacji</span>
       </a>
     </nav>
   </aside>
 
-  <!-- Content -->
   <section class="content">
-
-    <!-- ÔöÇÔöÇ EMBED CREATOR ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
     <div id="section-embed-creator" class="section-panel">
-
       <div class="section-header">
-        <h2>­čÄĘ Kreator Embeda</h2>
-        <p>Stw├│rz i wy┼Ťlij embed bezpo┼Ťrednio na wybrany kana┼é Discord. Obs┼éuguje formatowanie Markdown.</p>
+        <h2>­čÄĘ Kreator publikacji</h2>
+        <p>Tw├│rz publikacje w trybie Embedded lub Wiadomo┼Ť─ç. Mo┼╝esz pingn─ů─ç rol─Ö oraz opcjonalnie doda─ç grafik─Ö pod postem.</p>
       </div>
 
       <div class="creator-grid">
-
-        <!-- LEFT: Form -->
         <div class="creator-form-card">
+          <div class="form-group">
+            <label class="form-label">Tryb publikacji</label>
+            <div class="mode-tabs" id="mode-tabs">
+              <button type="button" class="mode-tab active" data-mode="embedded">Embedded</button>
+              <button type="button" class="mode-tab" data-mode="message">Wiadomo┼Ť─ç</button>
+            </div>
+          </div>
+
+          <div class="form-group" id="title-group">
+            <label class="form-label" for="title">Tytu┼é (opcjonalnie)</label>
+            <input id="title" name="title" type="text" class="form-input" placeholder="np. Wa┼╝na informacja">
+          </div>
+
+          <div class="form-group" id="color-group">
+            <label class="form-label">Kolor embeda</label>
+            <div class="color-swatches" id="color-swatches">
+              <div class="color-swatch active" data-color="czerwony" title="Czerwony"></div>
+              <div class="color-swatch" data-color="bia┼éy" title="Bia┼éy"></div>
+              <div class="color-swatch" data-color="szary" title="Szary"></div>
+              <div class="color-swatch" data-color="z┼éoty" title="Z┼éoty"></div>
+            </div>
+          </div>
 
-          <!-- Type Selector -->
           <div class="form-group">
-            <label class="form-label">Typ embeda</label>
-            <div class="type-tabs" id="type-tabs"></div>
+            <label class="form-label" for="content-textarea">Tre┼Ť─ç</label>
+            <textarea id="content-textarea" name="content" class="form-textarea" rows="5" placeholder="Wpisz tre┼Ť─ç publikacji..."></textarea>
           </div>
 
-          <!-- Dynamic Fields -->
-          <div id="dynamic-fields"></div>
+          <details class="form-group form-disclosure" id="ping-role-group">
+            <summary class="disclosure-summary">Ping roli przy publikacji</summary>
+            <div class="disclosure-content">
+              <label class="form-checkbox">
+                <input type="checkbox" id="ping-role-enabled">
+                <span>Wy┼Ťlij osobn─ů wiadomo┼Ť─ç @Rola przed publikacj─ů.</span>
+              </label>
+              <select id="ping-role-select" class="form-select" disabled>
+                <option value="">Wybierz rol─Ö do pingowania</option>
+              </select>
+            </div>
+          </details>
+
+          <details class="form-group form-disclosure" id="image-group">
+            <summary class="disclosure-summary">Wstaw grafik─Ö (opcjonalnie)</summary>
+            <div class="disclosure-content">
+              <p class="form-help">Obs┼éugiwane formaty: JPEG, PNG, GIF.</p>
+              <select id="image-mode-select" class="form-select">
+                <option value="none">Nie dodawaj grafiki</option>
+                <option value="library">Wybierz z biblioteki /img</option>
+                <option value="upload">Wgraj plik z komputera</option>
+              </select>
+
+              <div id="image-library-panel" class="image-option-panel" hidden>
+                <div class="image-grid" id="image-grid"></div>
+              </div>
+
+              <div id="image-upload-panel" class="image-option-panel" hidden>
+                <input type="file" id="image-upload-input" class="form-input" accept="image/png,image/jpeg,image/gif">
+                <p class="upload-file-name" id="upload-file-name">Nie wybrano pliku.</p>
+              </div>
+            </div>
+          </details>
 
-          <!-- Channel Selector -->
           <div class="form-group">
-            <label class="form-label" for="channel-select">­čôí Kana┼é docelowy</label>
+            <label class="form-label" for="channel-select">Kana┼é docelowy</label>
             <div class="select-wrapper">
               <select id="channel-select" class="form-select">
                 <option value="">┼üadowanie kana┼é├│w...</option>
@@ -82,84 +109,94 @@
             </div>
           </div>
 
-          <!-- Submit -->
           <button id="send-btn" class="btn-primary" disabled>
             <span class="btn-icon">­čôĄ</span>
-            <span id="send-btn-text">Wy┼Ťlij embed</span>
+            <span id="send-btn-text">Opublikuj</span>
           </button>
-
         </div>
 
-        <!-- RIGHT: Preview -->
-        <div class="creator-preview-card">
-          <div class="preview-header">
-            <span>­čĹü´ŞĆ Podgl─ůd</span>
-            <span class="preview-note">wygl─ůd przybli┼╝ony</span>
-          </div>
-          <div class="discord-mockup">
-            <div class="discord-message">
-              <div class="discord-avatar"><img src="/img/hussars_logo_80x80.png" alt="HusariaBot"></div>
-              <div class="discord-msg-content">
-                <span class="discord-bot-name">HusariaBot <span class="bot-badge">BOT</span></span>
-                <div class="discord-embed" id="embed-preview">
-                  <div class="embed-color-bar" id="embed-color-bar"></div>
-                  <div class="embed-body">
-                    <div class="embed-description" id="preview-description">
-                      Wype┼énij formularz, aby zobaczy─ç podgl─ůd embeda.
-                    </div>
-                    <div class="embed-fields" id="preview-fields"></div>
-                  </div>
-                </div>
+        <div class="creator-side-stack">
+          <div class="creator-tools-card">
+            <div class="form-group">
+              <label class="form-label">Narz─Ödzia edytora</label>
+              <div class="format-toolbar" id="format-toolbar">
+                <button type="button" class="toolbar-btn" data-wrap="**" title="Pogrubienie">B</button>
+                <button type="button" class="toolbar-btn" data-wrap="*" title="Kursywa">I</button>
+                <button type="button" class="toolbar-btn" data-wrap="__" title="Podkre┼Ťlenie">U</button>
+                <button type="button" class="toolbar-btn" data-wrap="~~" title="Przekre┼Ťlenie">S</button>
+                <button type="button" class="toolbar-btn" data-prefix="# " title="Nag┼é├│wek H1">H1</button>
+                <button type="button" class="toolbar-btn" data-prefix="## " title="Nag┼é├│wek H2">H2</button>
+                <button type="button" class="toolbar-btn" data-prefix="### " title="Nag┼é├│wek H3">H3</button>
+                <button type="button" class="toolbar-btn" data-prefix="> " title="Cytat">ÔŁŁ</button>
+                <button type="button" class="toolbar-btn" data-wrap="`" title="Kod inline">&lt;/&gt;</button>
               </div>
             </div>
-          </div>
-        </div>
 
-      </div><!-- /.creator-grid -->
-    </div><!-- /#section-embed-creator -->
+            <div class="mention-toolbox">
+              <div class="popover-header">
+                <span>Wstaw wzmiank─Ö</span>
+              </div>
 
-    <!-- ÔöÇÔöÇ IMAGE SENDER ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
-    <div id="section-image-sender" class="section-panel" style="display:none">
+              <div class="mention-section">
+                <span class="mention-label">Szybkie skr├│ty</span>
+                <div class="mention-list mention-list-inline" id="mention-quick-list">
+                  <button type="button" class="mention-chip mention-broadcast" data-token="@everyone">@everyone</button>
+                  <button type="button" class="mention-chip mention-broadcast" data-token="@here">@here</button>
+                </div>
+              </div>
 
-      <div class="section-header">
-        <h2>­čľ╝´ŞĆ Wy┼Ťlij obraz</h2>
-        <p>Wybierz obraz z biblioteki i wy┼Ťlij go na wybrany kana┼é Discord.</p>
-      </div>
+              <div class="mention-section">
+                <span class="mention-label">Kana┼éy</span>
+                <input type="text" id="mention-channel-search" class="popover-search" placeholder="Szukaj kana┼éu...">
+                <div class="mention-list" id="mention-channel-results"></div>
+              </div>
 
-      <div class="img-sender-grid">
+              <div class="mention-section">
+                <span class="mention-label">Role</span>
+                <input type="text" id="mention-role-search" class="popover-search" placeholder="Szukaj roli...">
+                <div class="mention-list" id="mention-role-results"></div>
+              </div>
 
-        <!-- Image grid -->
-        <div class="creator-form-card">
-          <div class="form-group">
-            <label class="form-label">Dost─Öpne obrazy</label>
-            <div id="image-grid" class="image-grid"></div>
+              <div class="mention-section">
+                <span class="mention-label">U┼╝ytkownicy</span>
+                <input type="text" id="mention-user-search" class="popover-search" placeholder="Szukaj u┼╝ytkownika...">
+                <div class="mention-user-results" id="mention-user-results"></div>
+              </div>
+            </div>
           </div>
 
-          <!-- Channel Selector -->
-          <div class="form-group">
-            <label class="form-label" for="img-channel-select">­čôí Kana┼é docelowy</label>
-            <div class="select-wrapper">
-              <select id="img-channel-select" class="form-select">
-                <option value="">┼üadowanie kana┼é├│w...</option>
-              </select>
+          <div class="creator-preview-card">
+            <div class="preview-header">
+              <span>Podgl─ůd</span>
+              <span class="preview-note">wygl─ůd przybli┼╝ony</span>
+            </div>
+            <div class="discord-mockup">
+              <div class="discord-message">
+                <div class="discord-avatar"><img src="/img/hussars_logo_80x80.png" alt="HusariaBot"></div>
+                <div class="discord-msg-content">
+                  <span class="discord-bot-name">HusariaBot <span class="bot-badge">BOT</span></span>
+                  <div class="discord-embed" id="embed-preview-container">
+                    <div class="embed-color-bar" id="embed-color-bar"></div>
+                    <div class="embed-body">
+                      <div class="embed-description" id="preview-description">Wpisz tre┼Ť─ç publikacji.</div>
+                    </div>
+                  </div>
+                  <div class="discord-plain-message" id="message-preview-container" style="display:none"></div>
+                  <div class="preview-image-block" id="preview-image-block" style="display:none">
+                    <img class="preview-image" id="preview-image" src="" alt="Podgl─ůd grafiki">
+                    <div class="preview-image-caption" id="preview-image-caption"></div>
+                  </div>
+                  <div class="preview-image-placeholder" id="preview-image-placeholder" style="display:none">­čľ╝´ŞĆ Wybierz grafik─Ö, aby zobaczy─ç podgl─ůd.</div>
+                </div>
+              </div>
             </div>
           </div>
-
-          <!-- Submit -->
-          <button id="img-send-btn" class="btn-primary" disabled>
-            <span class="btn-icon">­čôĄ</span>
-            <span id="img-send-btn-text">Wy┼Ťlij obraz</span>
-          </button>
         </div>
-
       </div>
-    </div><!-- /#section-image-sender -->
-
-  </section><!-- /.content -->
-
+    </div>
+  </section>
 </main>
 
-<!-- ÔöÇÔöÇ TOAST ÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇÔöÇ -->
 <div id="toast-container"></div>
 
 <script src="/public/js/app.js"></script>
